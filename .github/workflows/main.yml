name: Carga Dutra - APK 3D 99999

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      # ==========================================================
      # 1. BAIXAR O JOGO ORIGINAL
      # ==========================================================
      - name: Baixar jogo original
        run: |
          set -e

          curl -L --fail --max-time 120 \
            "https://cargadutra.netlify.app/" \
            -o original.html

          echo "HTML baixado:"
          wc -c original.html


      # ==========================================================
      # 2. NODE
      # ==========================================================
      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20


      # ==========================================================
      # 3. MODIFICAR OS MÓDULOS DO JOGO
      # ==========================================================
      - name: Modificar jogo
        run: |
          set -e

          python3 <<'PY'
          import re
          import base64
          import urllib.parse
          import os

          html = open(
              "original.html",
              "r",
              encoding="utf-8",
              errors="ignore"
          ).read()

          os.makedirs("web", exist_ok=True)

          def decode_data_url(url):
              header, data = url.split(",", 1)
              data = urllib.parse.unquote(data)

              if ";base64" in header.lower():
                  return base64.b64decode(data).decode(
                      "utf-8",
                      errors="ignore"
                  )

              return data

          def encode_data_url(js):
              encoded = base64.b64encode(
                  js.encode("utf-8")
              ).decode("ascii")

              return (
                  "data:text/javascript;charset=utf-8;base64,"
                  + encoded
              )

          scripts = re.findall(
              r'<script[^>]+src=["\']([^"\']+)["\']',
              html,
              re.I
          )

          data_scripts = [
              x for x in scripts
              if x.lower().startswith(
                  "data:text/javascript"
              )
          ]

          if not data_scripts:
              raise SystemExit(
                  "ERRO: script principal não encontrado."
              )

          principal_url = data_scripts[0]

          print("Script principal encontrado.")

          cache = {}
          timer_alterado = False

          pattern = re.compile(
              r'(?P<prefix>(?:from|import)\s*[\'"])'
              r'(?P<url>data:text/javascript[^\'"]+)'
              r'(?P<suffix>[\'"])',
              re.I
          )

          def process_module(url, nivel=0):

              global timer_alterado

              if url in cache:
                  return cache[url]

              js = decode_data_url(url)

              print(
                  "  " * nivel +
                  "Módulo:",
                  len(js),
                  "bytes"
              )

              def replace_import(match):

                  old_url = match.group("url")

                  new_url = process_module(
                      old_url,
                      nivel + 1
                  )

                  return (
                      match.group("prefix")
                      + new_url
                      + match.group("suffix")
                  )

              js = pattern.sub(
                  replace_import,
                  js
              )

              # ---------------------------------------------------
              # TIMER INTERNO
              # ---------------------------------------------------
              antigo = "this.state.elapsed += dt;"
              novo = "this.state.elapsed = 99999;"

              if antigo in js:

                  print(
                      "  " * nivel +
                      ">>> TIMER ENCONTRADO"
                  )

                  js = js.replace(
                      antigo,
                      novo,
                      1
                  )

                  timer_alterado = True

                  print(
                      "  " * nivel +
                      ">>> TIMER INTERNO = 99999"
                  )

              # ---------------------------------------------------
              # HUD
              # ---------------------------------------------------
              antigo_hud = (
                  "ui.hudTimer.textContent = "
                  "formatTime(hud.elapsed);"
              )

              novo_hud = (
                  "ui.hudTimer.textContent = '99999';"
              )

              if antigo_hud in js:

                  js = js.replace(
                      antigo_hud,
                      novo_hud,
                      1
                  )

                  print(
                      "  " * nivel +
                      ">>> HUD VISUAL = 99999"
                  )

              cache[url] = encode_data_url(js)

              return cache[url]

          principal_js = decode_data_url(
              principal_url
          )

          def replace_principal(match):

              old_url = match.group("url")

              new_url = process_module(
                  old_url,
                  1
              )

              return (
                  match.group("prefix")
                  + new_url
                  + match.group("suffix")
              )

          principal_js = pattern.sub(
              replace_principal,
              principal_js
          )

          if not timer_alterado:
              raise SystemExit(
                  "ERRO: timer não encontrado."
              )

          novo_principal = encode_data_url(
              principal_js
          )

          html_final = html.replace(
              principal_url,
              novo_principal,
              1
          )

          if html_final == html:
              raise SystemExit(
                  "ERRO: HTML não foi modificado."
              )

          open(
              "web/index.html",
              "w",
              encoding="utf-8"
          ).write(html_final)

          print("")
          print("======================================")
          print("JOGO MODIFICADO")
          print("======================================")
          print("Timer interno: 99999")
          print("Módulos processados:", len(cache))
          print("HTML final:", len(html_final))
          PY


      # ==========================================================
      # 4. CAPACITOR
      # ==========================================================
      - name: Instalar Capacitor
        run: |
          set -e

          npm init -y

          npm install \
            @capacitor/core \
            @capacitor/cli \
            @capacitor/android


      - name: Inicializar Capacitor
        run: |
          set -e

          npx cap init \
            "Carga Dutra" \
            "com.cargadutra.game" \
            --web-dir web


      - name: Adicionar Android
        run: |
          set -e

          npx cap add android


      # ==========================================================
      # 5. CONFIGURAÇÕES ANDROID / WEBVIEW
      # ==========================================================
      - name: Configurar Android WebView e WebGL
        run: |
          set -e

          python3 <<'PY'

          # ------------------------------------------------------
          # AndroidManifest
          # ------------------------------------------------------

          manifest = (
              "android/app/src/main/AndroidManifest.xml"
          )

          text = open(
              manifest,
              encoding="utf-8"
          ).read()

          permissions = [
              '<uses-permission android:name="android.permission.INTERNET" />',
              '<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />'
          ]

          for permission in permissions:

              if permission not in text:

                  text = text.replace(
                      "<manifest",
                      "<manifest\n    " + permission,
                      1
                  )

          # Hardware acceleration
          if "android:hardwareAccelerated" not in text:

              text = text.replace(
                  "<application",
                  '<application android:hardwareAccelerated="true"',
                  1
              )

          open(
              manifest,
              "w",
              encoding="utf-8"
          ).write(text)

          print("AndroidManifest configurado.")


          # ------------------------------------------------------
          # MainActivity
          # ------------------------------------------------------

          activity = (
              "android/app/src/main/java/"
              "com/cargadutra/game/MainActivity.java"
          )

          if not os.path.exists(activity):
              raise SystemExit(
                  "MainActivity.java não encontrado."
              )

          source = open(
              activity,
              encoding="utf-8"
          ).read()

          imports = """
          import android.os.Bundle;
          import android.webkit.WebSettings;
          import android.webkit.WebView;
          """

          if "android.webkit.WebSettings" not in source:

              source = source.replace(
                  "package com.cargadutra.game;",
                  "package com.cargadutra.game;\n" + imports,
                  1
              )

          # ------------------------------------------------------
          # Configuração do WebView
          # ------------------------------------------------------

          code = """

              @Override
              public void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);

                  WebView webView = getBridge().getWebView();

                  if (webView != null) {
                      WebSettings settings = webView.getSettings();

                      settings.setJavaScriptEnabled(true);
                      settings.setDomStorageEnabled(true);
                      settings.setDatabaseEnabled(true);

                      settings.setAllowFileAccess(true);
                      settings.setAllowContentAccess(true);

                      settings.setJavaScriptCanOpenWindowsAutomatically(true);
                      settings.setMediaPlaybackRequiresUserGesture(false);

                      settings.setLoadsImagesAutomatically(true);

                      settings.setSupportZoom(false);

                      webView.setLayerType(
                          WebView.LAYER_TYPE_HARDWARE,
                          null
                      );

                      WebView.setWebContentsDebuggingEnabled(false);
                  }
              }
          """

          # ------------------------------------------------------
          # Não adiciona duas vezes
          # ------------------------------------------------------

          if "setJavaScriptEnabled(true)" not in source:

              pos = source.rfind("}")

              if pos == -1:
                  raise SystemExit(
                      "Não foi possível modificar MainActivity."
                  )

              source = source[:pos] + code + "\n" + source[pos:]

          open(
              activity,
              "w",
              encoding="utf-8"
          ).write(source)

          print("MainActivity configurada para WebGL/hardware.")

          PY


      # ==========================================================
      # 6. COPIAR ASSETS
      # ==========================================================
      - name: Copiar jogo para Android
        run: |
          set -e

          npx cap copy android


      # ==========================================================
      # 7. JAVA 21
      # ==========================================================
      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21


      # ==========================================================
      # 8. ANDROID SDK
      # ==========================================================
      - name: Configurar Android SDK
        uses: android-actions/setup-android@v3


      # ==========================================================
      # 9. CORRIGIR KOTLIN
      # ==========================================================
      - name: Corrigir conflito Kotlin
        working-directory: android
        run: |
          set -e

          python3 <<'PY'

          path = "app/build.gradle"

          text = open(
              path,
              encoding="utf-8"
          ).read()

          bloco = """

          configurations.all {
              resolutionStrategy {
                  force 'org.jetbrains.kotlin:kotlin-stdlib:1.8.22'
                  force 'org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.8.22'
                  force 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.8.22'
              }
          }
          """

          if "kotlin-stdlib:1.8.22" not in text:
              text += bloco

          open(
              path,
              "w",
              encoding="utf-8"
          ).write(text)

          print("Kotlin configurado.")

          PY


      # ==========================================================
      # 10. COMPILAR
      # ==========================================================
      - name: Compilar APK
        working-directory: android
        run: |
          set -e

          chmod +x gradlew

          ./gradlew assembleDebug \
            --no-daemon \
            --stacktrace


      # ==========================================================
      # 11. PREPARAR APK
      # ==========================================================
      - name: Preparar APK
        run: |
          set -e

          mkdir -p final

          cp \
            android/app/build/outputs/apk/debug/app-debug.apk \
            final/carga-dutra-3d-99999.apk

          echo ""
          echo "======================================"
          echo "APK GERADO"
          echo "======================================"

          ls -lh final/carga-dutra-3d-99999.apk


      # ==========================================================
      # 12. DISPONIBILIZAR APK
      # ==========================================================
      - name: Disponibilizar APK
        uses: actions/upload-artifact@v4
        with:
          name: carga-dutra-3d-99999-apk
          path: final/carga-dutra-3d-99999.apk
          if-no-files-found: error
          retention-days: 7
